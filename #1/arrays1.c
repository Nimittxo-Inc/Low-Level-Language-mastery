#include <stdio.h>


int main(){
    int arr[10]={}, input_length, input_digit;

    printf("How many digits you want to enter !: ");
    scanf("%d", &input_length);

    for(int i=0; i<input_digit; ++i){
        printf("Enter the number: ");
        scanf("%d", &input_digit);
    }
    return 0;
}